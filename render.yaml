services:
  - type: web
    name: healththread-demo
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start -- --hostname 0.0.0.0 --port $PORT
    healthCheckPath: /
    envVars:
      - key: HEALTHTHREAD_PUBLIC_DEMO
        value: "1"
